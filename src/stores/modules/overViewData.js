import { defineStore } from "pinia";
import { ref } from 'vue';

/**
 * TODO: 所有的管理面板的统计数据都临时存储
 */
export const useOverViewData = defineStore("overViewData", () => {
  const dataSets = new Set();


  return {

  }
})
